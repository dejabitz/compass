package com.dejabit.compass.ui.theme

import androidx.compose.ui.graphics.Color

val TrueNorthAccent = Color(0xFFFF5722)       // Vibrant Safety Orange (Standard True North)
val MagneticNorthAccent = Color(0xFF00E5FF)   // Distinct Cyan (Magnetic North Fallback)
val SouthAccent = Color(0xFF9E9E9E)           // Muted Grey for South needle

val GhostMagText = Color(0x66FFFFFF)          // Subtle ghost indicator for MAG
val DialPrimaryTicks = Color(0xFFE0E0E0)      // Major tick marks (every 30 deg)
val DialSecondaryTicks = Color(0xFF4A4A4A)    // Minor tick marks (every 5/15 deg)
val DialCardinalText = Color(0xFFFFFFFF)

val BackgroundBlack = Color(0xFF000000)       // Battery-saving OLED pure black
val WarningYellow = Color(0xFFFFD600)         // Unreliable calibration warning

// Ambient / Always-On Display Colors
val AmbientWhite = Color(0xFFFFFFFF)
val AmbientGrey = Color(0xFF757575)
